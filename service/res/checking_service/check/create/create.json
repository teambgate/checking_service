{
        "request" : "post",
        "path"  : "/checking_service/check/{ID}/_create?parent={PARENT_ID}&routing=company",
        "data"  : {
                "check_in"      : "{CHECK_IN}",
                "check_out"     : "{CHECK_OUT}"
        }
}
